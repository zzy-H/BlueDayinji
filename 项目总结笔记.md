# 蓝牙热敏打印机项目总结笔记（Demo_dyj）

> 一句话版本：这是一个 **ESP32 做的便携蓝牙热敏打印机**，手机通过蓝牙把要打印的内容发给它，它就把内容"烧"在热敏纸上。本笔记用"总分总"的方式，让完全不懂技术的人也能看懂这个项目是怎么工作的。

---

## 第一部分：总 —— 这个项目是什么

**它是一台"手机就能控制的迷你打印机"。**

- 硬件核心：**ESP32 开发板**（一颗带蓝牙和 Wi-Fi 的芯片，这里只用它的蓝牙功能）
- 名字：蓝牙名称叫 **"MaoPaperang"**（手机搜索蓝牙能看到它）
- 干什么：手机 App 连上它 → 把文字/图片数据通过蓝牙发过去 → 它驱动**热敏打印头**把内容打印在热敏纸上（就是超市小票那种纸，不用墨水，靠加热变色）
- 和市面上的"喵喵机"是同一类产品

整体工作可以概括成一句话：

```
手机 App → 蓝牙 → ESP32 收到数据 → 解析协议 → 控制打印头加热 + 步进电机走纸 → 纸上出现内容
```

---

## 第二部分：分 —— 各个模块在干什么

### 2.1 硬件平台

| 项目 | 说明 |
|------|------|
| 主控芯片 | ESP32（乐鑫，带蓝牙 4.2） |
| 开发框架 | PlatformIO + Arduino 框架 |
| 调试串口 | 115200 波特率，用来打印调试信息 |

### 2.2 引脚分配（芯片哪些脚接什么）

**打印头（热敏头，通过 SPI 总线控制）：**

| 引脚号 | 作用 |
|--------|------|
| GPIO15 | SPI 时钟 CLK（节奏信号） |
| GPIO13 | SPI 数据 MOSI（把要打印的每一行数据送进打印头） |
| GPIO12 | 锁存 LAT（数据送完后"咔嚓"一下锁住，开始加热） |
| GPIO17 | 加热电源使能 VHEN（开/关打印头加热的总开关） |
| GPIO14/27/26/25/33/32 | 6 个加热选通 STB1~STB6（把 384 个发热点分成 6 段，轮流加热） |

**步进电机（负责走纸）：**

| 引脚号 | 作用 |
|--------|------|
| GPIO22 / 23 | 电机 A 相（AP / AM） |
| GPIO19 / 21 | 电机 B 相（BP / BM） |

**其他：**

| 引脚号 | 作用 |
|--------|------|
| GPIO5 | 物理按键（按下 → 打印一张测试页） |
| GPIO34 | 电池电压检测（ADC 采样，判断电量） |
| GPIO18 | 状态 LED（低电平点亮） |

### 2.3 软件模块（src 目录下每个文件干什么）

| 文件 | 模块 | 职责 |
|------|------|------|
| `main.cpp` | 主程序 | 初始化所有模块；主循环里**不断收蓝牙数据**，一帧一帧解析；检测按键、电量、蓝牙连接状态 |
| `bluetooth_protocol.cpp` | 蓝牙通信协议 | 定义并实现手机和打印机之间的"语言"：帧格式、CRC32 校验、几十种命令的处理（查版本、查电量、设置浓度……） |
| `tph.cpp` | 打印头驱动 TPH | 把收到的打印数据变成打印头的加热动作：SPI 送数据 → 锁存 → 6 段轮流加热 → 走纸，循环直到打完 |
| `motor.cpp` | 电机驱动 | 步进电机走纸（打印一行，纸往前走一行） |
| `led.cpp` | LED | 初始化状态灯 |
| `print_test.cpp` | 测试与状态 | 打印测试页、电池电压检测、纸张状态检测 |

### 2.4 每个模块的详细说明

#### ① 蓝牙通信模块（bluetooth_protocol）⭐ 最重要

它解决一个问题：**手机发的数据，打印机怎么知道每一段是什么意思？**

答案是"帧协议"：把数据包成固定格式的"信封"，见第三部分。

这个模块能处理的命令非常多，比如：
- 手机问：你是谁？（查型号/版本/序列号）
- 手机问：电量多少？（查电压）
- 手机说：打印这段内容（普通打印 / 压缩打印）
- 手机说：走几行纸（FEED_LINE）
- 手机说：打印一张测试页（PRINT_TEST_PAGE）
- 手机说：把加热浓度调高/调低（SET_HEAT_DENSITY）
- 手机说：多久没操作自动关机（SET_POWER_DOWN_TIME）

#### ② 打印头驱动模块（tph）

热敏打印头是一根"发热条"，上面有 **384 个发热点**（每行 48 字节 × 8 位 = 384 点）。
打印一行纸的流程是：

```
① 从内存取出一行数据（48 字节）
② SPI 串行送进打印头
③ 拉高 LAT 锁存 —— 384 个点"就位"
④ 打开加热电源 VHEN
⑤ 6 段（STB1~STB6）轮流加热 —— 为什么分段？因为 384 个点同时加热电流太大，
   分 6 段每段 64 点，电流小、电池扛得住
⑥ 电机走纸一行
⑦ 重复 ①~⑥，直到整张图打完
```

关键参数：
- 打印数据缓冲区最大 **30000 字节**（一次最多收约 30000/48 ≈ 620 行）
- 加热时间 PRINT_TIME = 3000（微秒级），浓度越大加热越久、颜色越深
- 图片规格：200 行 × 48 字节/行（IMG_LINES × IMG_BYTES）

#### ③ 电机模块（motor）

就是一个 4 线的**步进电机**，控制打印纸往前"走"。
`Motor_Feed(行数)` = 走纸多少行。打印头加热完一行，电机就把纸推出去一行，
这样下一行才能打到新位置 —— **"打一行、走一行"**。

#### ④ 按键与测试（print_test / 按键）

- 按下 GPIO5 的按键 → 打印一张**测试页**（自检：看看打印头是不是好的）
- 电池电压每 5 秒检测一次（ADC 读 GPIO34），电量低可以告诉手机

#### ⑤ LED（led）

一个状态指示灯，比如蓝牙连上了亮一下（代码里打印 "BT CONNECTED!"），打印时点亮。

---

## 第三部分：分 —— 通信方式详解（手机 ↔ 打印机怎么"说话"）

### 3.1 传输通道：蓝牙串口（SPP）

- ESP32 用自带的 `BluetoothSerial` 库，把自己变成一台"蓝牙串口设备"
- 手机 App 像连"蓝牙耳机"一样连上它（名称 **MaoPaperang**），连上后就是一条**透明的数据管道**：App 往里写字节，打印机读字节
- 调试口：USB 串口 115200，打印 "INIT OK"、"BT CONNECTED!" 等调试信息

### 3.2 数据帧格式（在蓝牙管道里跑的"信封"）

每一帧数据（信封）长这样：

```
┌──────┬─────────┬──────────┬──────────┬──────────────┬───────────┬──────┐
│ 起始 │ 命令类型 │ 帧序号    │ 数据长度 │ 数据内容      │ CRC32校验 │ 结束 │
│ 0x02 │ 1字节   │ 1字节     │ 2字节    │ N 字节       │ 4字节     │ 0x03 │
└──────┴─────────┴──────────┴──────────┴──────────────┴───────────┴──────┘
```

字段含义：
| 字段 | 大小 | 说明 |
|------|------|------|
| 起始字节 | 1 | 固定 `0x02`，告诉打印机"一帧开始了" |
| 命令类型 | 1 | 这帧是干什么的（打印？查电量？走纸？） |
| 帧序号 | 1 | 多帧传输时的编号，防乱序/重复 |
| 数据长度 | 2 | 后面数据有多少字节（小端：低字节在前） |
| 数据内容 | N | 真正的数据（打印内容、参数等），单帧上限 2047 字节 |
| CRC32 | 4 | 校验和 —— 检查传输过程中数据有没有"传错" |
| 结束字节 | 1 | 固定 `0x03`，表示"一帧结束了" |

**CRC32 校验**：打印机和手机共用一把"密钥"（初始值 `0x35769521`），
用标准 CRC32 算法算出整帧的指纹。接收方算一遍，和发来的 4 字节对比：
一样 → 数据完好；不一样 → 丢掉这帧（传输有损坏）。

**特殊处理：**
- 数据内容里如果出现 `0x02` 或 `0x03`，接收时要做**转义处理**（代码里用 readpos 8~12 的区域处理），防止数据中途被误判成"帧开始/帧结束"
- 大图打印时，手机把图片**分帧发送**（每帧 ≤2047 字节），打印机收完所有帧再拼起来打
- 支持**压缩打印**（PRINT_DATA_COMPRESS）：图片先压缩再发，省蓝牙流量、打得快

### 3.3 主要命令类型对照表

| 类型值 | 命令 | 方向 |
|--------|------|------|
| 0 | PRINT_DATA | 手机 → 打印机（普通打印数据） |
| 1 | PRINT_DATA_COMPRESS | 手机 → 打印机（压缩打印数据） |
| 4 / 5 | GET / SENT_VERSION | 问 / 答 版本号 |
| 6 / 7 | GET / SENT_MODEL | 问 / 答 型号（BK3432） |
| 8 / 9 | GET / SENT_BT_MAC | 问 / 答 蓝牙 MAC |
| 10 / 11 | GET / SENT_SN | 问 / 答 序列号（P1001705253855） |
| 12 / 13 | GET / SENT_STATUS | 问 / 答 打印机状态 |
| 14 / 15 | GET / SENT_VOLTAGE | 问 / 答 电压 |
| 16 / 17 | GET / SENT_BAT_STATUS | 问 / 答 电池状态 |
| 18 / 19 | GET / SENT_TEMP | 问 / 答 打印头温度 |
| 24 | SET_CRC_KEY | 设置新的 CRC 密钥 |
| 25 / 28 / 29 | SET / GET / SENT_HEAT_DENSITY | 设置 / 查询 加热浓度（打印深浅） |
| 26 | FEED_LINE | 走纸 N 行 |
| 27 | PRINT_TEST_PAGE | 打印测试页 |
| 30 / 31 / 32 | SET / GET / SENT_POWER_DOWN_TIME | 设置 / 查询 自动关机时间 |
| 42 / 43 / 44 | GET / SENT / SET_PAPER_TYPE | 查询 / 设置 纸张类型 |
| 45 / 46 | GET / SENT_COUNTRY_NAME | 问 / 答 国家（CN） |
| 48 / 49 | GET / SENT_DEV_NAME | 问 / 答 设备名（MaoPaperang） |

规律：**偶数 = 手机发问（GET/SET），奇数 = 打印机回答（SENT）**，一一对应。

---

## 第四部分：分 —— 一帧数据从进入到打印的完整流程

```
手机 App
   │ 发送 GET_SN 帧（想知道"你是谁"）
   ▼
蓝牙管道（SPP）
   ▼
main.cpp 主循环
   │ 一个字节一个字节读
   │ 见到 0x02 → 一帧开始，开始按位置读：类型→序号→长度→数据→CRC→0x03
   ▼
bluetooth_protocol.cpp
   │ 校验 CRC32：对 → 进入 paperang_process_data() 分发
   │ 打印数据帧 → 存入 printData 缓冲区（最多 30000 字节）
   │ 查询命令 → 组一帧 SENT_xxx 应答发回手机
   ▼
tph.cpp（打印头驱动）
   │ startPrint()：
   │   ① 开加热电源 VHEN
   │   ② 每行 48 字节经 SPI 送入打印头
   │   ③ LAT 锁存
   │   ④ STB1~STB6 六段轮流加热（每段 64 点）
   │   ⑤ Motor_Feed() 电机走纸一行
   │   ⑥ 循环 ②~⑤ 直到所有行打完
   ▼
热敏纸上出现内容 ✔
```

---

## 第五部分：总 —— 总结

**这个项目 = 一个完整的小型嵌入式产品原型。**

- 它把"**蓝牙通信**"、"**自定义协议**"、"**硬件驱动**"三件事串了起来：
  - 通信：蓝牙串口 + 帧协议（起始/类型/长度/数据/CRC/结束），用 CRC32 保证数据可靠；
  - 驱动：SPI 控制 384 点热敏打印头，6 段分时加热控制电流，步进电机逐行走纸；
  - 状态：电池检测、按键测试页、LED 指示、查询应答命令一应俱全。
- 对你（学习者）最有价值的三个知识点：
  1. **看 main.cpp** —— 学"协议解析状态机"怎么写（一个字节一个字节地吃数据）
  2. **看 bluetooth_protocol.cpp** —— 学"命令分发"和"CRC 校验"怎么写
  3. **看 tph.cpp** —— 学"硬件时序"怎么调（SPI、锁存、分时加热、走纸节奏）

一句话总结：**手机说话（蓝牙协议）→ 芯片翻译（协议解析）→ 打印头动手（加热 + 走纸）→ 纸上出字。**
