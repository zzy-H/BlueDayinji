# ESP32 蓝牙热敏打印机项目总结

---

## 一、项目简要概括

本项目基于 **ESP32** 微控制器和 **FPC21 热敏打印头**，实现了一个兼容**喵喵机（Paperang）APP** 的蓝牙热敏打印机。手机通过蓝牙连接打印机后，可以在 APP 中选择课程表、错题等图片并打印。电机驱动使用 **TC1508S H 桥**芯片控制步进电机走纸，SPI 通信使用 ESP32 的 **HSPI** 接口。开发环境为 **PlatformIO**，使用 **Arduino 框架**。

---

## 二、项目使用场景

1. **学生学习辅助**：手机拍照错题，打印错题本、课程表、知识点
2. **便签备忘**：打印购物清单、待办事项、留言条
3. **标签打印**：收纳标签、价格标签
4. **便携打印**：不用 Wi-Fi 和 USB 线，手机直连蓝牙即可打印，随身携带

---

## 三、硬件板子选型

| 元器件 | 型号 | 作用 |
|--------|------|------|
| 主控芯片 | **ESP32**（ESP32 Dev Module） | 双核240MHz，自带蓝牙+WiFi，Arduino生态成熟 |
| 打印头 | **FPC21** | 384个发热点，兼容喵喵机标准 |
| 电机驱动 | **TC1508S** | 双路H桥，驱动步进电机 |
| 步进电机 | **二相四线步进电机** | 精确走纸控制 |
| 电池 | 3.7V锂电池 + TP4056充电板 | 便携供电 |
| 升压 | MT3608升压模块 | 3.7V→5V/9V给打印头加热 |

**核心引脚接线：**

| GPIO | 连接 | 功能 |
|------|------|------|
| 13 | 打印头 DI | SPI数据输出 |
| 15 | 打印头 CLK | SPI时钟 |
| 12 | 打印头 LAT | 数据锁存（上升沿锁存） |
| 17 | 升压模块 EN | 加热电源使能 |
| 14/27/26/25/33/32 | 打印头 STB1~6 | 6段选通 |
| 22/23/19/21 | TC1508S | 步进电机A+B相 |

---

## 四、打印头型号及工作方式

**型号**：FPC21 热敏打印头

**参数**：
- 384 个发热点（每行）
- 48 字节 / 行（384÷8）
- 6 个 STB 选通段（每段控制 64 个点）
- 热敏纸遇热变黑

**工作流程**：

```
① 上电：VH_EN=1 → 开启加热电源
② 送数据：SPI串行发送48字节（384位）到打印头移位寄存器
③ 锁存：LAT上升沿 → 移位寄存器数据锁存到锁存器
④ 加热：6个STB同时拉高 → 发热点通电 → 热敏纸变黑
⑤ 关STB：延时2400µs后STB拉低
⑥ 走纸：步进电机转4步 → 移到下一行
⑦ 重复②~⑥直到全部行打完
```

---

## 五、步进电机型号及工作方式

**型号**：二相四线步进电机

**驱动芯片**：TC1508S（双路H桥）

**工作方式**：8拍半步进

```
节拍表：
Step 0: A+ ON,  A- OFF, B+ OFF, B- OFF
Step 1: A+ ON,  A- OFF, B+ ON,  B- OFF
Step 2: A+ OFF, A- OFF, B+ ON,  B- OFF
Step 3: A+ OFF, A- ON,  B+ ON,  B- OFF
Step 4: A+ OFF, A- ON,  B+ OFF, B- OFF
Step 5: A+ OFF, A- ON,  B+ OFF, B- ON
Step 6: A+ OFF, A- OFF, B+ OFF, B- ON
Step 7: A+ ON,  A- OFF, B+ OFF, B- ON
```

- 每次只改变一个线圈状态，运行平稳
- 电机引脚：AP=22、AM=23、BP=19、BM=21
- 每打印一行走 **4 步**（3步+1步保持位置）
- 步进间隔：800µs

---

## 六、可以实现的功能

| 编号 | 功能 | 说明 |
|------|------|------|
| 1 | 蓝牙自动连接 | 上电广播 "MaoPaperang"，手机APP自动搜索连接 |
| 2 | APP图像打印 | 手机选图→点打印→蓝牙发数据→打印头出图 |
| 3 | 按键测试打印 | 按板子上的按键，打印本地测试图片 |
| 4 | 分批加热保护 | 每批最多加热24个点，防止瞬间电流过大 |
| 5 | 6段同时加热 | 所有STB同时选通，提高打印速度 |
| 6 | 自动走纸 | 每行自动走纸4步，打完自动切纸 |
| 7 | 加热浓度可调 | APP可设置打印深浅（50%~100%） |
| 8 | 电池电量检测 | ADC读取电池电压，回传给APP显示 |
| 9 | 缺纸检测 | 检测是否安装热敏纸 |
| 10 | 蓝牙断连检测 | 实时监测蓝牙连接状态 |
| 11 | 协议指令应答 | 回复APP的版本、型号、状态等查询 |

---

## 七、做项目遇到的问题

### 问题1：GPIO6做SPI时钟导致看门狗复位

**现象**：上电几秒后ESP32一直重启。

**原因**：GPIO6是Flash时钟引脚，不能做SPI CLK，操作它会干扰Flash导致看门狗复位。

**解决**：改用**HSPI**（CLK=GPIO15, MOSI=GPIO13），不占用Flash引脚。

### 问题2：引脚全部定义错误，能连蓝牙但打印空白

**现象**：手机能连上"喵喵机"APP，点打印有走纸声音，但纸上什么也没有。

**原因**：对照别人正常的项目才发现，DI、LAT、STB1~6全部接错了引脚。

**解决**：逐行修正所有引脚定义，和正常项目的硬件连接保持一致。

### 问题3：电机反转（纸往里走）

**现象**：打印时纸往机器内部卷，而不是向外送出。

**原因**：8拍步进表的遍历方向反了。

**解决**：把遍历方向从递增改为递减，纸就往外走了。

### 问题4：编译报多重定义错误

**现象**：`multiple definition of 'crc32'`、`multiple definition of 'heat_density'` 等。

**原因**：同一个全局变量在多个 `.cpp` 文件中都定义了。

**解决**：每个全局变量只在 `tph.cpp` 中定义一次，其他文件通过 `.h` 的 `extern` 声明引用。

### 问题5：img.cpp数组声明错误

**现象**：`too many initializers for 'const uint8_t [9600]'`

**原因**：数组声明了 `[9600]` 但初始化器个数超过9600。

**解决**：改为 `const uint8_t img_data[] = {...}`，让编译器自动计算大小。

### 问题6：加热参数不当导致打印效果差

**现象**：打印颜色太浅或太深。

**原因**：加热时间、走纸步数等参数没调好。

**解决**：默认加热时间 3000µs（2400µs实际加热），每行4步走纸，APP可调加热浓度。

---

## 八、面试常问的问题

### Q1：这个项目实现的是什么？

**A**：这是一个基于ESP32的蓝牙热敏打印机，兼容喵喵机APP。手机选好图片点打印，蓝牙把图片数据发给ESP32，ESP32驱动打印头把图像印在热敏纸上。

### Q2：打印头是怎么工作的？

**A**：FPC21打印头内部有384位移位寄存器，6个STB各控制64个发热点。先SPI串行送48字节数据到移位寄存器，LAT上升沿锁存，然后6个STB同时拉高，让数据中为"1"的点通电发热，热敏纸遇热变黑。

### Q3：电机为什么走4步？

**A**：一行384个点对应的高度，刚好需要4个步进角度来移动一行。3步正常走，第4步保持位置。这样保证行与行之间没有缝隙也没有重叠。

### Q4：为什么不顺序加热而要分批加热？

**A**：384个点如果全同时加热，瞬间电流可达5A以上，可能烧坏电源或打印头。用`printpin=24`限制每批最多24个点同时加热，电流控制在安全范围内。多批加热完一行的时间是一样的，只是把大电流分散成了小电流。

### Q5：蓝牙连接稳定吗？大数据会不会丢？

**A**：稳定。因为用了逐字节状态机接收，不依赖阻塞函数，每字节立刻处理。ESP32的UART硬件有256字节FIFO，`BluetoothSerial`库还有更大的软件缓冲区。实测9600字节图片数据能完整接收。

### Q6：你在这个项目中主要负责了哪些？

**A**：整个项目从头到尾都是我一个人完成的。包括硬件选型、引脚接线调试、蓝牙协议解析、打印驱动编写、分批加热算法实现、以及排查GPIO冲突、引脚错误等多个硬件问题。

### Q7：如果让你改进，你会加什么？

**A**：① OTA蓝牙固件升级；② 长时间无操作自动休眠省电；③ 加入温度检测，过热自动减速；④ 支持压缩图片协议，传输更快。

---

## 九、蓝牙协议详解（喵喵机私有协议逆向）

> 这个项目要兼容喵喵机 APP，所以协议是**逆向分析**出来的——APP 下发什么格式、我们回什么格式，都要对上。

### 9.1 帧格式

```
[0]    START_BYTE (0x02)    帧头
[1]    packType             包类型（打印数据/命令/查询）
[2]    packIndex            包序号
[3:4]  dataLen              数据长度（小端 uint16）
[5..]  data                 数据内容（长度 = dataLen）
[..]   CRC32（4 字节小端）    数据校验
[..]   END_BYTE (0x03)      帧尾
```

### 9.2 包类型（packType）定义

| 值 | 名称 | 方向 | 说明 |
|----|------|------|------|
| 0 | PRINT_DATA | APP→设备 | 打印数据（图像像素）|
| 1 | PRINT_DATA_COMPRESS | APP→设备 | 压缩打印数据（暂未解压，同 0 处理）|
| 4/5 | GET/SENT_VERSION | 双向 | 查询/回复固件版本 |
| 6/7 | GET/SENT_MODEL | 双向 | 查询/回复型号（BK3432）|
| 10/11 | GET/SENT_SN | 双向 | 查询/回复序列号 |
| 12/13 | GET/SENT_STATUS | 双向 | 查询/回复状态 |
| 14/15 | GET/SENT_VOLTAGE | 双向 | 查询/回复电池电压(mV)|
| 16/17 | GET/SENT_BAT_STATUS | 双向 | 查询/回复电量百分比 |
| 18/19 | GET/SENT_TEMP | 双向 | 查询/回复打印头温度 |
| 24 | SET_CRC_KEY | APP→设备 | 设置 CRC 密钥 |
| 25/28/29 | SET/GET/SENT_HEAT_DENSITY | 双向 | 设置/查询/回复加热浓度 |
| 26 | FEED_LINE | APP→设备 | 送纸 |
| 27 | PRINT_TEST_PAGE | APP→设备 | 打印测试页 |
| 42~49 | 纸型/国家/设备名查询 | 双向 | 兼容查询 |

> **兼容性技巧**：APP 会查很多东西（版本/型号/SN/国家/设备名/未知命令 66/127），**全部按格式回一个假数据**（DEV_SN="P1001705253855"、DEV_MODEL="BK3432"、CMD_127="v3.38.19"）——APP 校验通过就认为设备正常。

### 9.3 CRC32 校验（查表法）

标准 CRC-32，多项式 `0xEDB88320`，初始密钥可被 APP 下发修改（SET_CRC_KEY）：

```c
// 生成 256 项查表（只生成一次）
void CRC32_Class::init(uint32_t key) {
    crc = key;
    if (!tableBuilt) {
        for (uint32_t i = 0; i < 256; i++) {
            uint32_t ch = i;
            for (int j = 0; j < 8; j++) {
                if (ch & 1) ch = 0xEDB88320 ^ (ch >> 1);
                else ch = ch >> 1;
            }
            table[i] = ch;
        }
        tableBuilt = true;
    }
}

// 计算 CRC32（初始值取反 → 逐字节查表 → 结果取反）
uint32_t CRC32_Class::calc(const uint8_t* data, uint32_t len) {
    uint32_t c = crc ^ 0xFFFFFFFF;
    for (uint32_t i = 0; i < len; i++)
        c = table[(c ^ data[i]) & 0xFF] ^ (c >> 8);
    return c ^ 0xFFFFFFFF;
}
```

> **为什么用查表法**：直接逐位计算 CRC 慢；查表法一个字节只需 1 次查表 + 几次异或，速度提升约 8 倍。打印数据量大，校验要快。

### 9.4 逐字节状态机接收（main.cpp 核心）

**为什么用状态机而不是阻塞接收**：蓝牙数据是**不定长 + 连续到达**的。如果用 `SerialBT.read()` 阻塞等待整帧，期间会丢数据。状态机逐字节处理，**每字节立刻分类存放**，不丢数据。

```c
// loop() 里：
while (SerialBT.available()) {
    uint8_t c = SerialBT.read();
    if (!gotStartByte) {
        if (c == START_BYTE) { gotStartByte=1; readpos=0; packHeader.dataLen=0; }
        continue;
    }
    switch (readpos) {
        case 0: packHeader.packType=c; readpos=1; break;   // 类型
        case 1: packHeader.packIndex=c; readpos=2; break;  // 序号
        case 2: packHeader.dataLen=c; readpos=3; break;    // 长度低
        case 3:                                            // 长度高 + 分流
            packHeader.dataLen |= (uint16_t)c << 8;
            if (packHeader.dataLen==0 || packHeader.dataLen>=2048) readpos=8;  // 非法长度跳过
            else { recv_count=0; recv_total=packHeader.dataLen; readpos=4; }
            break;
        case 4:                                            // 数据阶段
            if (packHeader.packType==PRINT_DATA || packHeader.packType==PRINT_DATA_COMPRESS) {
                if (printDataCount<PRINT_DATA_MAX) printData[printDataCount++]=c;  // 打印数据存大缓冲
            } else {
                if (dataPack_read_pos<2048) dataPack_read[dataPack_read_pos++]=c;  // 命令存小缓冲
            }
            recv_count++;
            if (recv_count>=recv_total) readpos=8;
            break;
        default:                                           // 帧尾（CRC4字节+END）
            readpos++;
            if (readpos>=13) { if(c==END_BYTE) paperang_process_data(); gotStartByte=0; readpos=0; }
            break;
    }
}
```

> **数据分流设计**：打印数据（PRINT_DATA）和命令数据分开存——打印数据量大（一张图 9600 字节），存到 `printData[PRINT_DATA_MAX]`；命令数据小，存到 `dataPack_read[2048]`。互不干扰。

---

## 十、关键函数详解（代码已加详细注释）

### 10.1 tph.cpp —— 打印驱动核心

| 函数 | 作用 |
|------|------|
| `Printer_Config()` | 打印头 + 电机引脚初始化（SPI 引脚由 HSPI 外设接管，不 pinMode）|
| `sendData(data)` | SPI 发送 48 字节到打印头 + LAT 上升沿锁存 |
| `startPrint()` | **核心**：整幅图像打印（分批加热 + 逐行走纸）|
| `goFront1/goFront` | 步进电机走 1 拍/N 拍（motorPos-- 反向遍历 = 出纸）|
| `clearSTB/clearData` | 关选通 / 清行缓存 |

**startPrint 分批加热算法（面试重点）**：

```c
// 每行 48 字节 = 384 个发热点
for (每行) {
    cache = -1;   // 本批起点
    while (cache != 0) {          // 处理完一整行
        pinnumber = 0;
        for (i=cache; i<48; i++) {
            pinnumber += 查表统计(printData[pointer+i]);   // 累加发热点数
            if (pinnumber <= printpin(24)) {
                printDatacache[i] = printData[pointer+i];   // 没超限加入本批
                if (i==47) cache=0;   // 到行尾
            } else { cache=i; break; }   // 超限 → 本字节作为下批起点
        }
        sendData(printDatacache);   // 送本批
        加热(PRINT_TIME * heat_density / 100);   // 加热时间×浓度
    }
    走纸 4 步;   // 3 步 + 1 步保持
}
```

**核心思想**：一行 384 点拆成若干批，**每批 ≤ 24 个发热点**（printpin 限制），防瞬间电流过大。多批加起来完成一行，总加热时间不变。

### 10.2 main.cpp —— 蓝牙接收 + 主循环

| 函数 | 作用 |
|------|------|
| `setup()` | 引脚/SPI/CRC32/蓝牙初始化，广播"MaoPaperang" |
| `loop()` | 蓝牙状态机接收 + 连接监测 + 电池上报 + 按键测试打印 |

### 10.3 bluetooth_protocol.cpp —— 协议层

| 函数 | 作用 |
|------|------|
| `paperang_process_data()` | 按 packType 分发处理（查询/设置/动作）|
| `paperang_send_msg(type, dat, len)` | 打包回复报文（START+type+len+dat+CRC32+END）|
| `paperang_send_ack(type)` | 简单 ACK（11 字节）|
| `CRC32_Class::init/calc` | CRC32 查表初始化/计算 |

### 10.4 print_test.cpp / motor.cpp —— 辅助

| 函数 | 作用 |
|------|------|
| `Print_test()` | 按键/命令触发，打印内置测试图片 |
| `BatteryPower()` | 电池 ADC 采样 → 电压 → 电量百分比 |
| `Motor_Feed(lines)` | 走纸 N 行（每行 8 拍，快速进纸）|

---

## 十一、代码文件结构

```
Demo_dyj/
├── platformio.ini          # PlatformIO 配置（esp32dev + Arduino 框架）
├── include/
│   ├── main.h              # 引脚定义（SPI/电机/STB/VHEN）+ 蓝牙对象
│   ├── bluetooth_protocol.h# 协议常量、帧头结构体、CRC32 类
│   ├── tph.h               # 打印头函数声明 + 打印参数宏
│   ├── motor.h             # 电机函数声明 + 全局变量 extern
│   ├── print_test.h        # 测试打印 + 电池函数声明
│   ├── img.h               # 测试图片声明
│   └── led.h               # LED 控制
└── src/
    ├── main.cpp            # setup/loop，蓝牙状态机接收
    ├── tph.cpp             # ★ 打印驱动核心（分批加热/SPI/走纸）
    ├── bluetooth_protocol.cpp  # 协议解析 + CRC32 + 回复打包
    ├── motor.cpp           # 电机送纸
    ├── print_test.cpp      # 测试打印 + 电池检测
    ├── led.cpp             # LED 控制
    └── img.cpp             # 内置测试图片数据
```

> **代码注释情况**：核心文件（tph.cpp/main.cpp/bluetooth_protocol.cpp/motor.cpp/print_test.cpp）已加详细中文注释，每个函数、每个关键变量、每个设计决策都有说明。面试前通读一遍即可脱稿讲清整个流程。

---

*文档整理日期: 2026-08-07（补充协议详解 + 关键函数 + 代码结构）*
